#lang racket
(provide (all-defined-out))

; these settings are for symbolic version
(define memory-book-size 1000)
(define storage-book-size 1000)
(define register-size 1000)
(define address-book-size 1000)