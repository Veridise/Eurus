#lang racket
(provide (all-defined-out))

; these settings are for symbolic version
(define memory-book-size 3000)
(define storage-book-size 3000)
(define register-size 3000)
(define address-book-size 3000)